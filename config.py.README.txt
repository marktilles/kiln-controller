My system controls two dofferent ovens, and one can swap between them with a link in the GUI and backend scripts. But you probably dont need this.

Just use one of the config.py files and tweak the variables.
